https://docs.unity3d.com/2017.1/Documentation/Manual/Components.html (older documentation 2017.1))
https://docs.unity3d.com/Manual/Components.html


Networking
    Use LZ4 compression for packets
        Should everything use LZ4 compression? What about small packets? Is it still worth it?
    Proper use of UDP/TCP

Todo
    Support Vulkan rendering backend
    assimp - Open Asset Import Library for model loading
    Keybind to action mapping system (like Unreal Engine how you can bind keys to actions)

Thoughts?
    Global event manager? register an event then they get polled every refresh - https://gamedev.stackexchange.com/a/59627
    Formatting library? - #include <fmt/format.h>
    miniaudio - audio library for AudioBackend

Player
    SendMessage

Universe
    Player[] - list of players in the universe
        GetPlayer(id: string): Player - get player by id
    Level[] - list of levels in the universe
        